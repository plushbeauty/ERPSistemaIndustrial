# 📝 IDENTIFICAÇÃO DE LEITURA E REVISÃO DE CÓDIGO:
# - Arquivo: .github/workflows/main.yml
# - Status Atual: Revisão 5
# - Total de Linhas Lido/Gerado: 46
# - Assinatura de Entrada: Não se aplica a YAML; validar name, on e permissions
# - Integração Concretizada: GitHub Actions sem cache npm e sem lockfile sintético; instala dependências, executa TypeScript e Build
name: ERP Industrial CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: erp-industrial-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: TypeScript + Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Instalar dependências
        run: npm install --legacy-peer-deps --no-audit --no-fund

      - name: TypeScript
        run: npm run type-check

      - name: Build
        run: npm run build
