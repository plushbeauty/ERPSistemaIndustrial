/**
 * =========================================================================
 * REVISÃO DE ENGENHARIA DE SOFTWARE INDUSTRIAL
 * Data/Hora: 24/09/2026 - 11:43 BRT
 * Desenvolvedor: IA Co-Pilot (Homologado por Fernando)
 * ID da Revisão: REV-065
 * Alterações: Corrigir horas_turno e aceitar IDs opcionais na timeline estrita.
 * Status do Build Local: Não executado — ambiente local sem acesso de rede ao repositório.
 * =========================================================================
 */

import { useEffect, useMemo, useState } from 'react'
import { Play } from 'lucide-react'
import type { FormEvent, ReactNode } from 'react'
import { supabase } from '../lib/supabaseClient'

type Tab='visao'|'novaop'|'pedidos'|'ops'|'materiais'|'producao'|'programacao'|'capacidade'|'qualidade'
type Modal='ajuda'|'op'|'programacao'|null
type Order={id:string;numero:number;cliente_id:string;status:string;total:number}
type Client={id:string;nome:string;codigo:string|null}
type OP={id:string;numero_op:string;produto_id:string|null;quantidade:number;status:string;pedido_venda_id:string|null;data_prevista:string|null}
type Product={id:string;codigo:string;nome:string;estoque_atual:number;categoria:string|null}
type Program={id:string;ordem_producao_id:string|null;maquina_id:string|null;produto_id:string|null;inicio_planejado:string;fim_planejado:string;quantidade_planejada:number;quantidade_produzida:number;quantidade_refugada:number;status:string;molde_id?:string|null;operador_frente_id?:string|null;operador_atras_id?:string|null;turnos?:number;horas_turno?:number;eficiencia_percent?:number;ciclo_seg?:number;cavidades_ativas?:number;setup_min?:number}
type Machine={id:string;codigo:string;nome:string;tipo:string|null;status:string}
type FichaOp={id:string;ficha_id:string;sequencia:number;operacao:string;maquina_id:string|null;molde_id:string|null;setup_min:number;ciclo_seg:number}
type Molde={id:string;codigo:string;nome:string;tipo:string;status:string;numero_cavidades:number;cavidades:number;cavidades_ativas:number}
type Employee={id:string;matricula:string;nome:string;cargo:string|null;status:string}
type Defect={id:string;ordem_producao_id:string;defeito:string;quantidade:number}
type Ficha={id:string;produto_id:string;versao:number;rendimento:number;ativa:boolean}
type FItem={id:string;ficha_id:string;componente_id:string;quantidade:number;perda_percentual:number}

function InlineIcon({name,size=18}:{name:string;size?:number}){const path=name==='Plus'?'M12 5v14M5 12h14':name==='X'?'M6 6l12 12M18 6 6 18':name==='CheckCircle2'?'M9 12l2 2 4-5':name==='XCircle'?'M8 8l8 8M16 8l-8 8':name==='ArrowLeft'?'M19 12H5M12 19l-7-7 7-7':name==='Search'?'M11 19a8 8 0 1 1 0-16 8 8 0 0 1 0 16Zm6-2 4 4':name==='RefreshCw'?'M20 11a8 8 0 0 0-14.8-4L3 9M3 4v5h5M4 13a8 8 0 0 0 14.8 4L21 15M21 20v-5h-5':'M5 12h14M12 5v14';return <svg viewBox="0 0 24 24" width={size} height={size} aria-hidden="true" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d={path}/></svg>}

const help:Record<Tab,{title:string;what:string;how:string;action:string}> = {
 visao:{title:'Visão geral',what:'É o painel de comando do PCP. Mostra a situação atual e orienta o usuário pelo fluxo completo.',how:'Comece pelos pedidos, confirme materiais, crie/libere a OP, programe máquina, aponte a produção e acompanhe qualidade.',action:'Use os atalhos abaixo para executar cada etapa.'},
 pedidos:{title:'Pedidos / Demanda',what:'Mostra pedidos comerciais que podem gerar necessidade de fabricação.',how:'Consulte um pedido, veja quantas OPs estão vinculadas e use a rotina Comercial para gerar as OPs necessárias.',action:'Aqui a função principal é consulta e rastreabilidade da demanda.'},
 ops:{title:'Ordens de Produção',what:'É o cadastro operacional da fabricação: produto, quantidade, prazo e status da OP.',how:'Clique em Nova OP, informe produto, quantidade e data prevista. Depois abra a OP para consultar materiais e apontar produção.',action:'Nova OP'},
 materiais:{title:'Materiais / MRP',what:'Compara a necessidade da ficha técnica da OP com o estoque atual.',how:'Selecione uma OP. O sistema calcula a necessidade dos componentes e mostra o saldo após a necessidade.',action:'Selecionar OP'},
 producao:{title:'Apontar produção',what:'Registra o que realmente saiu da fábrica, separando quantidade boa e defeituosa.',how:'Selecione a OP, informe quantidade encontrada e defeituosa, detalhe os defeitos e confirme. A rotina transacional atualiza produção/estoque/refugo.',action:'Conferir e lançar'},
 programacao:{title:'Programação / Gantt',what:'Organiza as OPs por máquina e horário.',how:'Clique em Programar OP, escolha OP, máquina, início, fim e quantidade. O registro fica disponível para acompanhamento.',action:'Programar OP'},
 capacidade:{title:'Capacidade / Máquinas',what:'Mostra as máquinas disponíveis e quantas programações cada uma possui.',how:'Use essa visão para identificar concentração de carga e apoiar o sequenciamento.',action:'Programar OP'},
 qualidade:{title:'Qualidade / Defeitos',what:'Consulta os defeitos apontados durante a produção.',how:'Os defeitos entram pelo apontamento. Depois podem ser tratados nas rotinas de Qualidade/RPNC.',action:'Apontar produção'}
}

export default function PCPIndustrial(){
 const [tab,setTab]=useState<Tab>(() => window.location.pathname === '/pcp/programacao' ? 'programacao' : 'visao'),[modal,setModal]=useState<Modal>(null)
 const [ops,setOps]=useState<OP[]>([]),[orders,setOrders]=useState<Order[]>([]),[clients,setClients]=useState<Client[]>([]),[products,setProducts]=useState<Product[]>([]),[programs,setPrograms]=useState<Program[]>([]),[machines,setMachines]=useState<Machine[]>([]),[defects,setDefects]=useState<Defect[]>([]),[fichas,setFichas]=useState<Ficha[]>([]),[fitems,setFitems]=useState<FItem[]>([]),[fichaOps,setFichaOps]=useState<FichaOp[]>([]),[molds,setMolds]=useState<Molde[]>([]),[employees,setEmployees]=useState<Employee[]>([])
 const [selectedOp,setSelectedOp]=useState(''),[found,setFound]=useState(''),[bad,setBad]=useState(''),[defectText,setDefectText]=useState(''),[destinationLocation,setDestinationLocation]=useState(''),[query,setQuery]=useState('')
 const [busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('')
 const [profileName,setProfileName]=useState('Usuário'),[clock,setClock]=useState(new Date())
 const [opForm,setOpForm]=useState({produto_id:'',cliente_id:'',quantidade:'',data_emissao:new Date().toISOString().slice(0,10),data_prevista:'',status:'pendente',prioridade:'ALTA',observacoes:''})
 const [progForm,setProgForm]=useState({ordem_producao_id:'',maquina_id:'',molde_id:'',operador_frente_id:'',operador_atras_id:'',inicio:'',fim:'',quantidade:'',status:'Programada',turnos:'1',horas_turno:'8',eficiencia:'85',ciclo_seg:'0',cavidades_ativas:'1',setup_min:'0'})

 async function load(){
  setBusy(true);setError('')
  try{
   const [o,op,cl,p,pr,m,d,f,fi,fo,mo,em]=await Promise.all([
    supabase.from('erp_pedidos_venda').select('id,numero,cliente_id,status,total').order('numero',{ascending:false}).limit(300),
    supabase.from('erp_clientes').select('id,nome,codigo').eq('ativo',true).order('nome').limit(2000),
    supabase.from('erp_ordens_producao').select('id,numero_op,produto_id,quantidade,status,pedido_venda_id,data_prevista').order('criado_em',{ascending:false}).limit(500),
    supabase.from('erp_produtos').select('id,codigo,nome,estoque_atual,categoria').eq('ativo',true).order('codigo').limit(1000),
    supabase.from('erp_pcp_programacoes').select('*').order('inicio_planejado').limit(1000),
    supabase.from('erp_maquinas').select('id,codigo,nome,tipo,status').not('status','eq','INATIVA').order('codigo'),
    supabase.from('erp_producao_defeitos').select('id,ordem_producao_id,defeito,quantidade').order('created_at',{ascending:false}).limit(500),
    supabase.from('erp_fichas_tecnicas').select('id,produto_id,versao,rendimento,ativa').eq('ativa',true),
    supabase.from('erp_ficha_itens').select('id,ficha_id,componente_id,quantidade,perda_percentual').order('sequencia'),
    supabase.from('erp_ficha_operacoes').select('id,ficha_id,sequencia,operacao,maquina_id,molde_id,setup_min,ciclo_seg').order('sequencia'),
    supabase.from('erp_moldes').select('id,codigo,nome,tipo,status,numero_cavidades,cavidades,cavidades_ativas').eq('ativo',true).order('codigo').limit(1000),
    supabase.from('erp_funcionarios').select('id,matricula,nome,cargo,status').eq('status','ativo').order('nome').limit(1000)
   ])
   for(const x of [o,op,cl,p,pr,m,d,f,fi,fo,mo,em]) if(x.error) throw x.error
   setOrders((o.data||[]) as Order[]);setClients((cl.data||[]) as Client[]);setOps((op.data||[]) as OP[]);setProducts((p.data||[]) as Product[]);setPrograms((pr.data||[]) as Program[]);setMachines((m.data||[]) as Machine[]);setDefects((d.data||[]) as Defect[]);setFichas((f.data||[]) as Ficha[]);setFitems((fi.data||[]) as FItem[]);setFichaOps((fo.data||[]) as FichaOp[]);setMolds((mo.data||[]) as Molde[]);setEmployees((em.data||[]) as Employee[])
  }catch(e){setError(e instanceof Error?e.message:'Falha ao carregar PCP.')}finally{setBusy(false)}
 }
 useEffect(()=>{void load()},[])
 useEffect(()=>{let alive=true;void supabase.auth.getUser().then(async({data})=>{if(!alive||!data.user)return;const r=await supabase.from('erp_usuarios').select('nome').eq('auth_user_id',data.user.id).eq('ativo',true).is('deleted_at',null).maybeSingle();if(alive)setProfileName(r.data?.nome||data.user.email?.split('@')[0]||'Usuário')});const id=window.setInterval(()=>setClock(new Date()),1000);return()=>{alive=false;window.clearInterval(id)}},[])

 const current=ops.find(o=>o.id===selectedOp)
 const currentPrograms=programs.filter(p=>p.ordem_producao_id===selectedOp)
 const productionFound=currentPrograms.reduce((s,p)=>s+Number(p.quantidade_produzida||0),0)
 const productionBad=currentPrograms.reduce((s,p)=>s+Number(p.quantidade_refugada||0),0)
 const productionGood=Math.max(0,productionFound-productionBad)
 const currentFicha=current?fichas.find(f=>f.produto_id===current.produto_id):undefined
 const materials=currentFicha?fitems.filter(i=>i.ficha_id===currentFicha.id).map(i=>({...i,product:products.find(p=>p.id===i.componente_id)})):[]
 const filteredOps=ops.filter(o=>!query||o.numero_op.toLowerCase().includes(query.toLowerCase())||String(o.status).toLowerCase().includes(query.toLowerCase()))
 const schedule=useMemo(()=>programs.map(p=>({...p,op:ops.find(o=>o.id===p.ordem_producao_id),machine:machines.find(m=>m.id===p.maquina_id),product:products.find(x=>x.id===p.produto_id)})),[programs,ops,machines,products])

 function openNewOP(){setError('');setMessage('');setOpForm({produto_id:'',cliente_id:'',quantidade:'',data_emissao:new Date().toISOString().slice(0,10),data_prevista:'',status:'pendente',prioridade:'ALTA',observacoes:''});setModal('op')}
 function processForOp(opId:string){const op=ops.find(x=>x.id===opId);const ficha=op?fichas.find(f=>f.produto_id===op.produto_id):undefined;return ficha?fichaOps.filter(x=>x.ficha_id===ficha.id).sort((a,b)=>a.sequencia-b.sequencia):[]}
 function openProgram(opId=selectedOp){setError('');setMessage('');const op=ops.find(o=>o.id===opId);const process=processForOp(opId)[0];const mold=process?.molde_id||'';const machine=process?.maquina_id||'';const cav=mold?(molds.find(x=>x.id===mold)?.cavidades_ativas||molds.find(x=>x.id===mold)?.numero_cavidades||1):1;const cycle=process?.ciclo_seg||0;const setup=process?.setup_min||0;setProgForm({ordem_producao_id:opId,maquina_id:machine,molde_id:mold,operador_frente_id:'',operador_atras_id:'',inicio:'',fim:'',quantidade:opId?String(op?.quantidade||''):'',status:'Programada',turnos:'1',horas_turno:'8',eficiencia:'85',ciclo_seg:String(cycle),cavidades_ativas:String(cav),setup_min:String(setup)});setModal('programacao')}

 async function createOP(e:FormEvent){
  e.preventDefault();const quantidade=Number(opForm.quantidade)
  if(!opForm.produto_id||quantidade<=0){setError('Produto e quantidade maior que zero são obrigatórios.');return}
  setBusy(true);setError('');setMessage('')
  try{
   const product=products.find(p=>p.id===opForm.produto_id)
   const numero='OP-'+new Date().toISOString().slice(0,10).replace(/-/g,'')+'-'+String(Date.now()).slice(-6)
   const empresa=await supabase.rpc('erp_current_empresa_id');if(empresa.error||!empresa.data)throw empresa.error||new Error('Empresa ERP não identificada.')
   const r=await supabase.from('erp_ordens_producao').insert({empresa_id:empresa.data,numero_op:numero,produto_id:opForm.produto_id,cliente_id:opForm.cliente_id||null,quantidade,status:opForm.status,prioridade:opForm.prioridade,data_prevista:opForm.data_prevista||null,observacoes:opForm.observacoes||null}).select('id,numero_op').single().select('id,numero_op').single()
   if(r.error) throw r.error
   setMessage('OP '+(r.data?.numero_op||numero)+' criada para '+(product?.codigo||product?.nome||'produto')+'.')
   setModal(null);setSelectedOp(r.data?.id||'');await load()
  }catch(e){setError(e instanceof Error?e.message:'Não foi possível criar a OP.')}
  finally{setBusy(false)}
 }

 async function createProgram(e:FormEvent){
  e.preventDefault();const qty=Number(progForm.quantidade),cycle=Number(progForm.ciclo_seg),cav=Math.max(1,Number(progForm.cavidades_ativas)),eff=Math.min(100,Math.max(1,Number(progForm.eficiencia))),turnos=Math.max(1,Number(progForm.turnos)),horasTurno=Math.max(.5,Number(progForm.horas_turno)),setup=Math.max(0,Number(progForm.setup_min))
  if(!progForm.ordem_producao_id||!progForm.maquina_id||!progForm.inicio||!progForm.fim||qty<=0){setError('OP, máquina, início, fim e quantidade são obrigatórios.');return}
  if(new Date(progForm.fim)<=new Date(progForm.inicio)){setError('O fim deve ser posterior ao início.');return}
  const op=ops.find(x=>x.id===progForm.ordem_producao_id)
  const machine=machines.find(x=>x.id===progForm.maquina_id)
  if(machine?.status?.toUpperCase().includes('MANUT')){setError('A máquina selecionada está em manutenção e não pode receber programação.');return}
  const mold=molds.find(x=>x.id===progForm.molde_id)
  if(mold?.status?.toUpperCase().includes('MANUT')){setError('O molde selecionado está em manutenção e não pode receber programação.');return}
  const start=new Date(progForm.inicio)
  const end=new Date(progForm.fim)
  const activePrograms=programs.filter(p=>p.status.toLowerCase()!=='cancelada')
  const overlaps=(p:Program)=>start<new Date(p.fim_planejado)&&end>new Date(p.inicio_planejado)
  const machineConflict=activePrograms.some(p=>p.maquina_id===progForm.maquina_id&&overlaps(p))
  if(machineConflict){setError('Conflito de horário: a injetora já possui uma programação nesse intervalo.');return}
  const moldConflict=Boolean(progForm.molde_id)&&activePrograms.some(p=>p.molde_id===progForm.molde_id&&overlaps(p))
  if(moldConflict){setError('Conflito de horário: o mesmo molde já está programado nesse intervalo.');return}
  const dayKey=(value:Date)=>value.toISOString().slice(0,10)
  const sameMachineDay=activePrograms.filter(p=>p.maquina_id===progForm.maquina_id&&dayKey(new Date(p.inicio_planejado))===dayKey(start))
  const distinctMolds=new Set(sameMachineDay.map(p=>p.molde_id).filter((id):id is string=>Boolean(id)))
  if(progForm.molde_id)distinctMolds.add(progForm.molde_id)
  if(distinctMolds.size>2){setError('A mesma injetora suporta no máximo dois moldes distintos no mesmo dia.');return}
  setBusy(true);setError('');setMessage('')
  const detailed={ordem_producao_id:progForm.ordem_producao_id,maquina_id:progForm.maquina_id,produto_id:op?.produto_id||null,inicio_planejado:progForm.inicio,fim_planejado:progForm.fim,quantidade_planejada:qty,quantidade_produzida:0,quantidade_refugada:0,status:progForm.status,molde_id:progForm.molde_id||null,operador_frente_id:progForm.operador_frente_id||null,operador_atras_id:progForm.operador_atras_id||null,turnos,horas_turno:horasTurno,eficiencia_percent:eff,ciclo_seg:cycle,cavidades_ativas:cav,setup_min:setup}
  try{
   let r=await supabase.from('erp_pcp_programacoes').insert(detailed).select('id').single()
   if(r.error&&/column .* does not exist|schema cache/i.test(r.error.message)){const legacy={ordem_producao_id:detailed.ordem_producao_id,maquina_id:detailed.maquina_id,produto_id:detailed.produto_id,inicio_planejado:detailed.inicio_planejado,fim_planejado:detailed.fim_planejado,quantidade_planejada:detailed.quantidade_planejada,quantidade_produzida:0,quantidade_refugada:0,status:detailed.status};r=await supabase.from('erp_pcp_programacoes').insert(legacy).select('id').single();if(!r.error)setMessage('Programação gravada, mas a estrutura detalhada ainda depende da migração PCP v2.')}
   if(r.error) throw r.error
   setMessage('Programação criada. A barra agora representa máquina, dias, molde, ciclo, cavidades, equipe e quantidade.')
   setModal(null);setSelectedOp(progForm.ordem_producao_id);await load()
  }catch(e){setError(e instanceof Error?e.message:'Não foi possível programar a OP.')}
  finally{setBusy(false)}
 }

 async function confirmProduction(){
  if(!selectedOp){setError('Selecione uma OP.');return}
  const f=Number(found),b=Number(bad)
  if(f<0||b<0||b>f){setError('Quantidade encontrada/defeituosa inválida.');return}
  const defectsJson=defectText.split('\n').map(x=>x.trim()).filter(Boolean).map(x=>{const parts=x.split(':');return{defeito:parts[0].trim(),quantidade:Number(parts[1]||0),observacao:parts.slice(2).join(':').trim()||null}}).filter(x=>x.defeito&&x.quantidade>0)
  setBusy(true);setError('')
  try{
   const {data,error}=await supabase.rpc('erp_registrar_conferencia_producao',{p_ordem_producao_id:selectedOp,p_quantidade_encontrada:f,p_quantidade_defeituosa:b,p_defeitos:defectsJson,p_localizacao_destino_id:destinationLocation||null,p_acabamento:false,p_observacao:'Conferência realizada no PCP'})
   if(error)throw error
   const r=data as {quantidade_boa?:number;saldo_producao?:number}
   setMessage('Conferência registrada: '+Number(r.quantidade_boa||0)+' boas, '+b+' em refugo. Saldo de produção: '+Number(r.saldo_producao||0)+'.')
   setFound('');setBad('');setDefectText('');await load()
  }catch(e){setError(e instanceof Error?e.message:'Não foi possível registrar a produção.')}finally{setBusy(false)}
 }

 const statusSummary=useMemo(()=>{const m:Record<string,number>={};for(const o of ops){const k=String(o.status||'Sem status');m[k]=(m[k]||0)+1}return Object.entries(m).sort((a,b)=>b[1]-a[1]).slice(0,6)},[ops])
 const productionDays=useMemo(()=>{const map:Record<string,{date:string;good:number;scrap:number}>={};for(const p of programs){const d=String(p.inicio_planejado||'').slice(0,10);if(!d)continue;map[d]??={date:d,good:0,scrap:0};map[d].good+=Math.max(0,Number(p.quantidade_produzida||0)-Number(p.quantidade_refugada||0));map[d].scrap+=Number(p.quantidade_refugada||0)}return Object.values(map).sort((a,b)=>a.date.localeCompare(b.date)).slice(-7)},[programs])
 const maxProduction=Math.max(1,...productionDays.map(x=>x.good+x.scrap))
 const openOps=ops.filter(o=>!['concluída','concluida','finalizada','cancelada','concluído','concluido'].includes(String(o.status||'').toLowerCase())).length
 const totalGood=programs.reduce((s,p)=>s+Math.max(0,Number(p.quantidade_produzida||0)-Number(p.quantidade_refugada||0)),0)
 const totalScrap=programs.reduce((s,p)=>s+Number(p.quantidade_refugada||0),0)
 function selectTab(next:Tab){setTab(next);setError('');setMessage('')}

 const tabs:[Tab,string,string][]=[['visao','Visão geral','Gauge'],['novaop','Criar Nova OP','Plus'],['pedidos','Pedidos / Demanda','ClipboardList'],['ops','Ordens de Produção','Factory'],['materiais','Materiais / MRP','Package'],['producao','Apontar produção','Play'],['programacao','Programação / Gantt','CalendarDays'],['capacidade','Capacidade / Máquinas','Wrench'],['qualidade','Qualidade / Defeitos','ShieldCheck']]

 return <main className="pcp-modern-page pcp-industrial-shell">
  <aside className="pcp-sidebar">
   <div className="pcp-sidebar-brand"><img src="/logo-industrial.svg" alt="SGQ ERP Industrial"/><div><strong>SGQ ERP</strong><small>PCP INDUSTRIAL</small></div></div>
   <button className="pcp-side-item" onClick={()=>location.href='/erp-industrial'}><span>⌂</span> DASHBOARD</button>
   <div className="pcp-side-section">PLANEJAMENTO E CONTROLE</div>
   <button className={`pcp-side-item ${tab==='visao'?'active':''}`} onClick={()=>selectTab('visao')}><span>▦</span> VISÃO GERAL</button>
   <button className={`pcp-side-item ${tab==='novaop'?'active':''}`} onClick={()=>selectTab('novaop')}><span>＋</span> CRIAR NOVA OP</button>
   <button className="pcp-side-item" onClick={()=>selectTab('pedidos')}><span>▤</span> PEDIDOS / DEMANDA</button>
   <button className="pcp-side-item" onClick={()=>selectTab('ops')}><span>□</span> ORDENS DE PRODUÇÃO</button>
   <button className="pcp-side-item" onClick={()=>selectTab('materiais')}><span>◇</span> MATERIAIS / MRP</button>
   <button className="pcp-side-item" onClick={()=>selectTab('programacao')}><span>◫</span> PROGRAMAÇÃO / GANTT</button>
   <button className="pcp-side-item" onClick={()=>selectTab('capacidade')}><span>▥</span> CAPACIDADE / MÁQUINAS</button>
   <button className="pcp-side-item" onClick={()=>selectTab('producao')}><span>▶</span> CHÃO DE FÁBRICA</button>
   <button className="pcp-side-item" onClick={()=>location.href='/pcp/paradas'}><span>Ⅱ</span> PARADAS / SETUP</button>
   <button className="pcp-side-item" onClick={()=>selectTab('qualidade')}><span>✓</span> QUALIDADE / DEFEITOS</button>
   <div className="pcp-side-section">PLANEJAMENTO AVANÇADO</div>
   <button className="pcp-side-item" onClick={()=>location.href='/pcp/planejamento'}><span>◈</span> MPS / CAPACIDADE</button>
   <button className="pcp-side-item" onClick={()=>location.href='/mrp'}><span>◌</span> MRP MULTINÍVEL</button>
   <button className="pcp-side-item" onClick={()=>location.href='/configuracao-lote-pcp'}><span>⚙</span> CONFIGURAÇÃO DE LOTE</button>
   <button className="pcp-side-item" onClick={()=>location.href='/qualidade/refugos'}><span>△</span> REFUGO / RETRABALHO</button>
   <div className="pcp-side-spacer"/>
   <button className="pcp-side-item" onClick={()=>location.href='/manual-usuario'}><span>?</span> MANUAL</button>
  </aside>
  <section className="pcp-main">
   <header className="pcp-topbar">
    <div className="pcp-top-title"><button className="pcp-top-tablet" onClick={()=>location.href='/demo/erp-industrial'}>▦ TABLET</button><div><strong>PCP Industrial</strong><small>PCP • PLANEJAMENTO E CONTROLE DA PRODUÇÃO</small></div></div>
    <div className="pcp-top-actions"><span className="pcp-supabase-dot"><i/> DADOS SUPABASE</span><span className="pcp-top-user"><i/>{profileName}</span><span className="pcp-top-clock">{clock.toLocaleDateString('pt-BR')} • {clock.toLocaleTimeString('pt-BR')}</span><button className="pcp-top-light">☼ Light</button><button className="pcp-top-exit" onClick={()=>void supabase.auth.signOut().then(()=>location.replace('/login'))}>Sair</button></div>
   </header>
   <div className="pcp-page-head"><div><span>PCP • PLANEJAMENTO E CONTROLE DA PRODUÇÃO</span><h1>{tab==='visao'?'DASHBOARD':tab==='novaop'?'NOVA ORDEM DE PRODUÇÃO':help[tab].title.toUpperCase()}</h1><p>{tab==='visao'?'Visão geral da programação, produção, capacidade e qualidade da fábrica.':tab==='novaop'?'Cadastro operacional da OP, explosão de materiais e roteiro de produção.':help[tab].what}</p></div></div>
   {(message||error)&&<div className={error?'error':'notice'} style={{margin:'0 28px 12px'}}>{error||message}</div>}
   <div className="pcp-content">
  {tab==='novaop'&&<NovaOPScreen products={products} clients={clients} machines={machines} fichas={fichas} fitems={fitems} fichaOps={fichaOps} onCancel={()=>selectTab('visao')} onSave={createOP} busy={busy} form={opForm} setForm={setOpForm}/>}\n\n  {tab==='visao'&&<section>
   <div className="quality-kpis">
    <article><span>PEDIDOS</span><strong>{orders.length}</strong><small>demanda registrada</small></article>
    <article><span>OPs ABERTAS</span><strong>{openOps}</strong><small>ordens em execução</small></article>
    <article><span>PRODUÇÃO BOA</span><strong>{totalGood.toLocaleString('pt-BR')}</strong><small>quantidade apontada</small></article>
    <article><span>REFUGO / DEFEITOS</span><strong>{totalScrap.toLocaleString('pt-BR')}</strong><small>quantidade registrada</small></article>
   </div>

   <div className="pcp-dashboard-grid">
    <article className="pcp-dash-card">
     <h2>Pedidos por Status</h2>
     {statusSummary.length ? statusSummary.map(([name,n])=><div className="pcp-status-row" key={name}><span>{name}</span><div className="pcp-status-bar"><i style={{width:(n/Math.max(1,ops.length))*100+'%'}}/></div><b>{n}</b></div>) : <div className="pcp-empty">Nenhuma OP real encontrada.</div>}
    </article>
    <article className="pcp-dash-card">
     <h2>Produção (últimos 7 dias)</h2>
     {productionDays.length ? <div className="pcp-prod-bars">{productionDays.map(d=><div className="pcp-prod-day" key={d.date}><b>{(d.good+d.scrap).toLocaleString('pt-BR')}</b><div className="pcp-prod-bar" style={{height:Math.max(4,((d.good+d.scrap)/maxProduction)*120)}}/><small>{new Date(d.date+'T12:00:00').toLocaleDateString('pt-BR',{day:'2-digit',month:'2-digit'})}</small></div>)}</div> : <div className="pcp-chart-empty">Nenhuma produção apontada no período disponível.</div>}
    </article>
    <article className="pcp-dash-card">
     <h2>Alertas do PCP</h2>
     <div className="pcp-alert-list">
      {openOps>0&&<div className="pcp-alert"><span className="pcp-alert-icon">!</span><div><strong>{openOps} OPs em aberto</strong><small>Acompanhar programação e produção.</small></div></div>}
      {totalScrap>0&&<div className="pcp-alert"><span className="pcp-alert-icon">!</span><div><strong>{totalScrap.toLocaleString('pt-BR')} peças em refugo</strong><small>Verificar Qualidade / Refugo.</small></div></div>}
      {programs.filter(p=>String(p.status||'').toLowerCase().includes('program')).length===0&&<div className="pcp-alert"><span className="pcp-alert-icon">i</span><div><strong>Sem programação ativa</strong><small>Use Programação / Gantt para carregar a fábrica.</small></div></div>}
      {!openOps&&!totalScrap&&programs.length>0&&<div className="pcp-alert"><span className="pcp-alert-icon">✓</span><div><strong>PCP sem alertas calculados</strong><small>Dados atuais não indicam pendências.</small></div></div>}
      {!ops.length&&!programs.length&&<div className="pcp-chart-empty">Sem registros reais para gerar alertas.</div>}
     </div>
    </article>
   </div>

   <div className="pcp-dashboard-tables">
    <article className="pcp-mini-table"><h2>Últimas Ordens de Produção</h2><table><thead><tr><th>OP</th><th>Produto</th><th>Quantidade</th><th>Status</th></tr></thead><tbody>{ops.slice(0,5).map(o=><tr key={o.id}><td><b>{o.numero_op}</b></td><td>{products.find(p=>p.id===o.produto_id)?.codigo||'—'}</td><td>{Number(o.quantidade||0).toLocaleString('pt-BR')}</td><td>{o.status}</td></tr>)}{!ops.length&&<tr><td colSpan={4}>Nenhuma ordem real cadastrada.</td></tr>}</tbody></table></article>
    <article className="pcp-mini-table"><h2>Programações recentes</h2><table><thead><tr><th>Máquina</th><th>OP</th><th>Início</th><th>Status</th></tr></thead><tbody>{schedule.slice(0,5).map(p=><tr key={p.id}><td>{p.machine?.codigo||'—'}</td><td>{p.op?.numero_op||'—'}</td><td>{new Date(p.inicio_planejado).toLocaleString('pt-BR',{day:'2-digit',month:'2-digit',hour:'2-digit',minute:'2-digit'})}</td><td>{p.status}</td></tr>)}{!schedule.length&&<tr><td colSpan={4}>Nenhuma programação real cadastrada.</td></tr>}</tbody></table></article>
   </div>

   <div className="pcp-flow">
    {(['pedidos','ops','materiais','programacao','producao','qualidade'] as Tab[]).map((id,i)=><button key={id} onClick={()=>selectTab(id)}><b>{i+1}</b><strong>{help[id].title}</strong><small>{help[id].what}</small><em>{help[id].action} →</em></button>)}
   </div>
  </section>}

  {tab==='pedidos'&&<section><Table title="Pedidos que alimentam o PCP" cols={['Pedido','Status','Total','OPs vinculadas']} rows={orders.filter(o=>!query||String(o.numero).includes(query)).map(o=>[o.numero,o.status,o.total,ops.filter(x=>x.pedido_venda_id===o.id).length])} search={query} setSearch={setQuery}/></section>}

  {tab==='ops'&&<section className="crud-list">
   <div className="crud-list-head"><div><strong>Ordens de Produção</strong><small>Cadastre, consulte e abra uma OP para continuar o processo.</small></div><div className="pcp-list-actions"><div className="module-search"><InlineIcon name="Search" size={16}/><input placeholder="OP / status" value={query} onChange={e=>setQuery(e.target.value)}/></div><button className="primary-v2" onClick={openNewOP}><InlineIcon name="Plus" size={17}/> Nova OP</button></div></div>
   <div className="crud-table-wrap"><table><thead><tr><th>OP</th><th>Produto</th><th>Quantidade</th><th>Status</th><th>Prevista</th><th>Ações</th></tr></thead><tbody>{filteredOps.map(o=><tr key={o.id}><td><strong>{o.numero_op}</strong></td><td>{products.find(p=>p.id===o.produto_id)?.codigo||'—'} • {products.find(p=>p.id===o.produto_id)?.nome||'Produto não localizado'}</td><td>{o.quantidade}</td><td>{o.status}</td><td>{o.data_prevista||'—'}</td><td className="pcp-row-actions"><button className="secondary-v2" onClick={()=>{setSelectedOp(o.id);selectTab('materiais')}}>Materiais</button><button className="secondary-v2" onClick={()=>{setSelectedOp(o.id);selectTab('producao')}}>Apontar</button><button className="secondary-v2" onClick={()=>openProgram(o.id)}>Programar</button></td></tr>)}</tbody></table></div>
  </section>}

  {tab==='materiais'&&<section>
   <div className="erp-card" style={{padding:18,marginBottom:14}}><span className="v2-eyebrow">BOM / MRP</span><h2>Necessidade de materiais</h2><p className="pcp-help-text">Selecione uma OP. O sistema usa a ficha técnica ativa e compara a necessidade com o estoque atual.</p><select value={selectedOp} onChange={e=>setSelectedOp(e.target.value)}><option value="">Selecione a OP</option>{ops.map(o=><option key={o.id} value={o.id}>{o.numero_op} • {o.quantidade}</option>)}</select></div>
   <Table title={current?'Necessidade calculada para '+current.numero_op:'Selecione uma OP para calcular'} cols={['Componente','Necessidade','Estoque atual','Saldo após necessidade','Situação']} rows={materials.map(i=>{const need=Number(i.quantidade)*Number(current?.quantidade||0)*(1+Number(i.perda_percentual||0)/100);const stock=Number(i.product?.estoque_atual||0);return[i.product?.codigo+' • '+i.product?.nome,need,stock,stock-need,stock>=need?'ATENDE':'FALTA']})} search="" setSearch={()=>{}}/>
  </section>}

  {tab==='producao'&&<section className="pcp-production-modern">
   <section className="pcp-modern-panel"><div className="pcp-modern-panel-head"><div><span>CONFERÊNCIA DE PRODUÇÃO</span><h2>Entrada real do que saiu da fábrica</h2><p>Registre o resultado da OP. O lançamento usa a rotina transacional do ERP.</p></div><InlineIcon name="Play" size={24}/></div>
    <div className="pcp-form-grid-modern">
     <label><span>OP</span><select value={selectedOp} onChange={e=>setSelectedOp(e.target.value)}><option value="">Selecione a OP</option>{ops.map(o=><option key={o.id} value={o.id}>{o.numero_op} • planejado {o.quantidade}</option>)}</select></label>
     <label><span>Quantidade Encontrada</span><input type="number" min="0" step="any" value={found} onChange={e=>setFound(e.target.value)} inputMode="decimal"/></label>
     <label><span>Quantidade Defeituosa</span><input type="number" min="0" step="any" value={bad} onChange={e=>setBad(e.target.value)} inputMode="decimal"/></label>
     <label><span>Localização de Destino</span><input value={destinationLocation} onChange={e=>setDestinationLocation(e.target.value)} placeholder="ID da localização"/></label>
     <label className="wide"><span>Detalhes de Qualidade</span><textarea rows={4} value={defectText} onChange={e=>setDefectText(e.target.value)} placeholder="Defeito:quantidade:observação — um por linha"/></label>
    </div>
    <div className="pcp-modern-form-footer"><span>Boa = encontrada − defeituosa.</span><button className="pcp-conferir" disabled={busy} onClick={()=>void confirmProduction()}><InlineIcon name="Play" size={21}/>{busy?'Lançando…':'CONFERIR E LANÇAR'}</button></div>
   </section>
   <section className="pcp-modern-panel"><div className="pcp-modern-panel-head"><div><span>STATUS DA OP</span><h2>{current?current.numero_op:'Selecione uma OP'}</h2><p>{current?'Indicadores atuais das programações.':'Selecione uma OP para acompanhar.'}</p></div><InlineIcon name="Gauge" size={24}/></div>
    <div className="pcp-stock-modern-grid"><article><InlineIcon name="ClipboardList"/><small>Planejada</small><strong>{current?.quantidade??0}</strong><em>Plano da OP</em></article><article><InlineIcon name="Package"/><small>Encontrada</small><strong>{productionFound}</strong><em>Apontada</em></article><article className="red"><InlineIcon name="XCircle"/><small>Defeituosa</small><strong>{productionBad}</strong><em>Refugo</em></article><article className="green"><InlineIcon name="CheckCircle2"/><small>Boa</small><strong>{productionGood}</strong><em>Quantidade boa</em></article></div>
   </section>
  </section>}

  {tab==='programacao'&&<ProgramacaoTimeline programs={schedule} machines={machines} molds={molds} employees={employees} onProgram={openProgram}/>} 

  {tab==='capacidade'&&<section><div className="pcp-capacity-grid">{machines.map(m=><article key={m.id}><div><span>{m.codigo}</span><strong>{m.nome}</strong><small>{m.tipo||'Máquina'} • {m.status}</small></div><b>{programs.filter(p=>p.maquina_id===m.id).length} programação(ões)</b><button onClick={()=>openProgram()}><InlineIcon name="CalendarDays" size={16}/> Programar</button></article>)}</div></section>}

  {tab==='qualidade'&&<Table title="Defeitos enviados pela produção" cols={['Defeito','Quantidade','OP']} rows={defects.filter(d=>!query||d.defeito.toLowerCase().includes(query.toLowerCase())||d.ordem_producao_id.includes(query)).map(d=>[d.defeito,d.quantidade,d.ordem_producao_id])} search={query} setSearch={setQuery}/>}

  {modal==='ajuda'&&<Modal title="Como usar o PCP Industrial" onClose={()=>setModal(null)}><div className="pcp-help-modal">{tabs.map(([id,label,I])=><button key={id} onClick={()=>{selectTab(id);setModal(null)}}><InlineIcon name={I} size={20}/><div><strong>{label}</strong><p>{help[id].what}</p><small>Como usar: {help[id].how}</small></div><span>→</span></button>)}</div><div className="pcp-info"><InlineIcon name="CheckCircle2" size={20}/><div><strong>Fluxo recomendado</strong><p>Pedido → OP → MRP → Programação → Apontamento → Estoque/Qualidade. Cada etapa tem uma finalidade; não é necessário preencher tudo de uma vez.</p></div></div></Modal>}

  {modal==='op'&&<Modal title="Nova Ordem de Produção" onClose={()=>setModal(null)}><form className="pcp-modal-form" onSubmit={createOP}><label>Produto<select value={opForm.produto_id} onChange={e=>setOpForm(v=>({...v,produto_id:e.target.value}))}><option value="">Selecione o produto</option>{products.map(p=><option key={p.id} value={p.id}>{p.codigo} • {p.nome}</option>)}</select></label><label>Quantidade<input type="number" min="0.0001" step="any" value={opForm.quantidade} onChange={e=>setOpForm(v=>({...v,quantidade:e.target.value}))}/></label><label>Data prevista<input type="date" value={opForm.data_prevista} onChange={e=>setOpForm(v=>({...v,data_prevista:e.target.value}))}/></label><label>Status<select value={opForm.status} onChange={e=>setOpForm(v=>({...v,status:e.target.value}))}><option>Planejada</option><option>Aguardando PCP</option><option>Em produção</option><option>Concluída</option></select></label><label className="wide">Observações<textarea value={opForm.observacoes} onChange={e=>setOpForm(v=>({...v,observacoes:e.target.value}))}/></label><div className="pcp-modal-footer"><button type="button" className="secondary-v2" onClick={()=>setModal(null)}>Cancelar</button><button className="primary-v2" disabled={busy}>{busy?'Criando…':'Criar OP'}</button></div></form></Modal>}

  {modal==='programacao'&&<Modal title="Programar Ordem de Produção" onClose={()=>setModal(null)}><form className="pcp-modal-form" onSubmit={createProgram}><label>Ordem de Produção<select value={progForm.ordem_producao_id} onChange={e=>{const id=e.target.value;const op=ops.find(o=>o.id===id);const pr=processForOp(id)[0];const mold=pr?.molde_id||'';const cav=mold?(molds.find(x=>x.id===mold)?.cavidades_ativas||molds.find(x=>x.id===mold)?.numero_cavidades||1):1;setProgForm(v=>({...v,ordem_producao_id:id,maquina_id:pr?.maquina_id||v.maquina_id,molde_id:mold,quantidade:String(op?.quantidade||''),ciclo_seg:String(pr?.ciclo_seg||0),cavidades_ativas:String(cav),setup_min:String(pr?.setup_min||0)}))}}><option value="">Selecione a OP</option>{ops.map(o=><option key={o.id} value={o.id}>{o.numero_op} • {o.quantidade}</option>)}</select></label><label>Máquina<select value={progForm.maquina_id} onChange={e=>setProgForm(v=>({...v,maquina_id:e.target.value}))}><option value="">Selecione a máquina</option>{machines.map(m=><option key={m.id} value={m.id}>{m.codigo} • {m.nome}</option>)}</select></label><label>Molde<select value={progForm.molde_id} onChange={e=>{const id=e.target.value;const m=molds.find(x=>x.id===id);setProgForm(v=>({...v,molde_id:id,cavidades_ativas:String(m?.cavidades_ativas||m?.numero_cavidades||1)}))}}><option value="">Sem molde selecionado</option>{molds.map(m=><option key={m.id} value={m.id}>{m.codigo} • {m.nome} • {m.cavidades_ativas||m.numero_cavidades} cav.</option>)}</select></label><label>Operador frente<select value={progForm.operador_frente_id} onChange={e=>setProgForm(v=>({...v,operador_frente_id:e.target.value}))}><option value="">Não definido</option>{employees.map(e=><option key={e.id} value={e.id}>{e.nome} • {e.cargo||'Operador'}</option>)}</select></label><label>Operador atrás<select value={progForm.operador_atras_id} onChange={e=>setProgForm(v=>({...v,operador_atras_id:e.target.value}))}><option value="">Não definido</option>{employees.map(e=><option key={e.id} value={e.id}>{e.nome} • {e.cargo||'Operador'}</option>)}</select></label><label>Início<input type="datetime-local" value={progForm.inicio} onChange={e=>setProgForm(v=>({...v,inicio:e.target.value}))}/></label><label>Fim<input type="datetime-local" value={progForm.fim} onChange={e=>setProgForm(v=>({...v,fim:e.target.value}))}/></label><label>Quantidade planejada<input type="number" min="0.0001" step="any" value={progForm.quantidade} onChange={e=>setProgForm(v=>({...v,quantidade:e.target.value}))}/></label><label>Ciclo (segundos)<input type="number" min="0" step="0.001" value={progForm.ciclo_seg} onChange={e=>setProgForm(v=>({...v,ciclo_seg:e.target.value}))}/></label><label>Cavidades ativas<input type="number" min="1" step="1" value={progForm.cavidades_ativas} onChange={e=>setProgForm(v=>({...v,cavidades_ativas:e.target.value}))}/></label><label>Setup / troca de molde (min)<input type="number" min="0" step="0.1" value={progForm.setup_min} onChange={e=>setProgForm(v=>({...v,setup_min:e.target.value}))}/></label><label>Turnos/dia<input type="number" min="1" step="1" value={progForm.turnos} onChange={e=>setProgForm(v=>({...v,turnos:e.target.value}))}/></label><label>Horas por turno<input type="number" min=".5" step=".5" value={progForm.horas_turno} onChange={e=>setProgForm(v=>({...v,horas_turno:e.target.value}))}/></label><label>Eficiência planejada %<input type="number" min="1" max="100" step="1" value={progForm.eficiencia} onChange={e=>setProgForm(v=>({...v,eficiencia:e.target.value}))}/></label><label>Status<select value={progForm.status} onChange={e=>setProgForm(v=>({...v,status:e.target.value}))}><option>Programada</option><option>Em produção</option><option>Concluída</option><option>Cancelada</option></select></label><div className="pcp-modal-footer"><button type="button" className="secondary-v2" onClick={()=>setModal(null)}>Cancelar</button><button className="primary-v2" disabled={busy}>{busy?'Gravando…':'Gravar programação'}</button></div></form></Modal>}

  <style>{`
.pcp-newop{display:flex;flex-direction:column;gap:14px}.pcp-newop-card{background:#fff;border:1px solid #d7e3ea;border-radius:10px;box-shadow:0 3px 12px rgba(18,59,80,.06);overflow:hidden}.pcp-newop-card>header{padding:13px 16px;border-bottom:1px solid #e3ebef;display:flex;align-items:center;gap:10px}.pcp-newop-card>header b{width:26px;height:26px;border-radius:50%;background:#2563eb;color:#fff;display:grid;place-items:center;font-size:12px}.pcp-newop-card>header strong{font-size:14px;color:#123b50}.pcp-newop-grid{padding:15px;display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.pcp-newop-grid label{display:flex;flex-direction:column;gap:6px;font-size:10px;font-weight:900;color:#425a67}.pcp-newop-grid input,.pcp-newop-grid select,.pcp-newop-grid textarea{width:100%;box-sizing:border-box;border:1px solid #c8d8e0;border-radius:6px;background:#fff;color:#0f172a;padding:10px;font-size:12px;outline:none}.pcp-newop-grid input:focus,.pcp-newop-grid select:focus,.pcp-newop-grid textarea:focus{border-color:#2563eb;box-shadow:0 0 0 3px rgba(37,99,235,.1)}.pcp-newop-grid .wide{grid-column:1/-1}.pcp-newop-table{width:100%;border-collapse:collapse;font-size:11px}.pcp-newop-table th,.pcp-newop-table td{padding:10px 12px;text-align:left;border-bottom:1px solid #e8eef2}.pcp-newop-table th{background:#f7fafc;color:#64748b;font-size:9px;text-transform:uppercase}.pcp-newop-ok{font-weight:900;color:#16845b}.pcp-newop-warn{font-weight:900;color:#b45309}.pcp-newop-route{display:flex;flex-direction:column;padding:6px 15px 15px}.pcp-newop-route-row{display:grid;grid-template-columns:54px 1fr 190px 150px;gap:10px;align-items:center;padding:10px 0;border-bottom:1px solid #e8eef2;font-size:11px}.pcp-newop-route-row b{color:#2563eb}.pcp-newop-route-row strong{color:#123b50}.pcp-newop-actions{display:flex;justify-content:flex-end;gap:9px;padding:13px 15px;border-top:1px solid #e3ebef;background:#f8fafc}.pcp-newop-actions button{border:1px solid #c8d8e0;background:#fff;border-radius:6px;padding:10px 16px;font-weight:900;cursor:pointer}.pcp-newop-actions .primary{background:#2563eb;color:#fff;border-color:#2563eb}.pcp-newop-help{padding:12px 15px;color:#64748b;font-size:11px;background:#f8fafc;border-bottom:1px solid #e3ebef}@media(max-width:950px){.pcp-newop-grid{grid-template-columns:1fr 1fr}.pcp-newop-route-row{grid-template-columns:45px 1fr 130px}}@media(max-width:700px){.pcp-newop-grid{grid-template-columns:1fr}.pcp-newop-route-row{grid-template-columns:36px 1fr}.pcp-newop-route-row span{display:none}}.pcp-modern-page{min-height:100vh;background:#F4FBFD;color:#123B50;display:flex;font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}.pcp-sidebar{position:sticky;top:0;width:245px;min-width:245px;height:100vh;box-sizing:border-box;background:#061C2D;color:#fff;padding:10px;display:flex;flex-direction:column;overflow:auto}.pcp-sidebar-brand{height:58px;display:flex;align-items:center;gap:10px;padding:0 7px;margin-bottom:10px;border-bottom:1px solid rgba(255,255,255,.12)}.pcp-sidebar-brand img{width:48px;height:48px;object-fit:contain}.pcp-sidebar-brand strong{display:block;font-size:18px}.pcp-sidebar-brand small{display:block;color:#62C7D6;font-size:9px;font-weight:900;letter-spacing:.12em;margin-top:2px}.pcp-side-section{font-size:9px;font-weight:950;letter-spacing:.12em;color:#6FA5B6;padding:15px 12px 6px}.pcp-side-item{width:100%;min-height:42px;border:0;border-radius:9px;background:transparent;color:#F7FBFC;text-align:left;display:flex;align-items:center;gap:11px;padding:0 12px;font-size:12px;font-weight:850;cursor:pointer}.pcp-side-item span{width:20px;text-align:center;font-size:17px;color:#CBE6EC}.pcp-side-item:hover{background:#0E3348}.pcp-side-item.active{background:#1674D1;box-shadow:0 6px 16px rgba(22,116,209,.28)}.pcp-side-spacer{flex:1}.pcp-main{min-width:0;flex:1;background:#F4F8FC}.pcp-topbar{height:58px;box-sizing:border-box;background:#fff;border-bottom:1px solid #D9E4EA;display:flex;align-items:center;justify-content:space-between;padding:0 18px 0 26px;gap:14px}.pcp-top-title{display:flex;align-items:center;gap:14px}.pcp-top-title>div{display:flex;flex-direction:column}.pcp-top-title strong{font-size:21px;color:#123B50}.pcp-top-title small{margin-top:5px;color:#2D8DB8;font-size:9px;font-weight:950;letter-spacing:.13em}.pcp-top-tablet{background:#ED6B0B;border:1px solid #F08B3C;color:#fff;border-radius:8px;min-height:34px;padding:0 11px;font-weight:950;cursor:pointer}.pcp-top-actions{display:flex;align-items:center;gap:8px;font-size:11px;white-space:nowrap}.pcp-supabase-dot,.pcp-top-user{display:inline-flex;align-items:center;gap:6px;border:1px solid #C9DDE5;background:#F1FAFC;padding:8px 10px;border-radius:7px;font-weight:900}.pcp-top-user{background:#fff}.pcp-supabase-dot i,.pcp-top-user i{width:8px;height:8px;border-radius:50%;background:#16845B;display:inline-block}.pcp-top-clock{color:#526B77}.pcp-top-light,.pcp-top-exit{min-height:34px;border:1px solid #C9DDE5;background:#fff;border-radius:7px;padding:0 10px;color:#234653;font-weight:800}.pcp-top-exit{color:#A62E32;border-color:#E6B4B7;cursor:pointer}.pcp-page-head{padding:13px 28px 10px;background:#F4F8FC}.pcp-page-head>div>span{color:#2D8DB8;font-size:9px;font-weight:950;letter-spacing:.14em}.pcp-page-head h1{font-size:25px;line-height:1;margin:7px 0 5px;color:#102B3B}.pcp-page-head p{margin:0;color:#5C7280;font-size:12px}.pcp-content{padding:0 28px 28px}.pcp-content>section{margin-top:10px}.quality-kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px}.quality-kpis article{background:#fff;border:1px solid #D7E3EA;border-radius:11px;padding:14px 16px;box-shadow:0 4px 12px rgba(20,60,80,.06)}.quality-kpis span{font-size:10px;color:#637985;font-weight:900}.quality-kpis strong{display:block;color:#122B3B;font-size:28px;margin-top:5px}.quality-kpis small{display:block;color:#728791;margin-top:3px}.pcp-flow{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-top:12px}.pcp-flow button{background:#fff;border:1px solid #D7E3EA;border-radius:11px;padding:15px;text-align:left;cursor:pointer;box-shadow:0 4px 12px rgba(20,60,80,.05)}.pcp-flow b{width:28px;height:28px;border-radius:50%;background:#1674D1;color:#fff;display:inline-flex;align-items:center;justify-content:center}.pcp-flow strong{display:block;font-size:16px;margin-top:9px}.pcp-flow small{display:block;color:#637985;line-height:1.4;margin-top:5px}.pcp-flow em{display:block;color:#2D8DB8;font-style:normal;font-weight:900;margin-top:7px;font-size:11px}.pcp-chart-empty,.pcp-empty{padding:30px 12px;text-align:center;color:#728791;font-size:12px}.pcp-dashboard-grid{display:grid;grid-template-columns:1.2fr 1fr .95fr;gap:12px;margin-top:12px}.pcp-dash-card{background:#fff;border:1px solid #D7E3EA;border-radius:11px;padding:16px;box-shadow:0 4px 12px rgba(20,60,80,.05)}.pcp-dash-card h2{font-size:16px;margin:0 0 12px;color:#123B50}.pcp-status-row{display:grid;grid-template-columns:92px 1fr 34px;align-items:center;gap:8px;margin:10px 0;font-size:11px}.pcp-status-bar{height:9px;border-radius:20px;background:#E7EFF3;overflow:hidden}.pcp-status-bar i{display:block;height:100%;border-radius:20px;background:#2D8DB8}.pcp-prod-bars{height:160px;display:flex;align-items:flex-end;gap:8px;padding-top:10px}.pcp-prod-day{flex:1;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%;gap:5px}.pcp-prod-bar{width:100%;max-width:32px;border-radius:5px 5px 0 0;background:#2D8DB8;min-height:3px}.pcp-prod-day small{font-size:9px;color:#6B818C}.pcp-prod-day b{font-size:9px;color:#315462}.pcp-alert-list{display:grid}.pcp-alert{display:flex;align-items:center;gap:10px;padding:10px 0;border-bottom:1px solid #E5EDF1;font-size:11px}.pcp-alert:last-child{border-bottom:0}.pcp-alert-icon{width:30px;height:30px;border-radius:8px;background:#FFF2E4;color:#D67B12;display:grid;place-items:center;font-weight:950}.pcp-alert strong{display:block;font-size:12px}.pcp-alert small{display:block;color:#70848E;margin-top:2px}.pcp-dashboard-tables{display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-top:12px}.pcp-mini-table{background:#fff;border:1px solid #D7E3EA;border-radius:11px;overflow:hidden}.pcp-mini-table h2{font-size:15px;margin:0;padding:14px 16px;border-bottom:1px solid #E2EBEF}.pcp-mini-table table{width:100%;border-collapse:collapse;font-size:10px}.pcp-mini-table th,.pcp-mini-table td{padding:9px 11px;text-align:left;border-bottom:1px solid #EDF2F4}.pcp-mini-table th{font-size:9px;color:#637985;background:#F8FBFC}.pcp-mini-table td{color:#314E5B}.pcp-section-toolbar{display:flex;align-items:center;justify-content:space-between;gap:12px;background:#fff;border:1px solid #D7E3EA;border-radius:11px;padding:13px 15px;margin-bottom:12px}.pcp-list-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap}.pcp-modern-panel,.pcp-timeline-card,.pcp-capacity-grid article{background:#fff;border:1px solid #D7E3EA;border-radius:11px;box-shadow:0 4px 12px rgba(20,60,80,.05)}.pcp-modern-panel{padding:18px}.pcp-modern-panel-head{display:flex;justify-content:space-between;gap:14px;margin-bottom:16px}.pcp-modern-panel-head span{display:block;color:#2D8DB8;font-size:9px;font-weight:950;letter-spacing:.14em}.pcp-modern-panel-head h2{font-size:19px;margin:5px 0;color:#123B50}.pcp-modern-panel-head p{margin:0;color:#637985;font-size:12px;line-height:1.5}.pcp-form-grid-modern,.pcp-modal-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.pcp-form-grid-modern label,.pcp-modal-form label{display:flex;flex-direction:column;gap:6px;color:#17333F;font-size:12px;font-weight:850}.pcp-form-grid-modern input,.pcp-form-grid-modern select,.pcp-form-grid-modern textarea,.pcp-modal-form input,.pcp-modal-form select,.pcp-modal-form textarea{width:100%;box-sizing:border-box;min-height:42px;border:1px solid #C7D8DF;border-radius:7px;background:#F8FBFC;color:#17333F;padding:9px 11px;font-size:13px}.pcp-production-modern{display:grid;grid-template-columns:1.2fr .8fr;gap:12px}.pcp-stock-modern-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.pcp-stock-modern-grid article{padding:14px;border:1px solid #D7E3EA;border-radius:10px;background:#F9FCFD}.pcp-stock-modern-grid small,.pcp-stock-modern-grid strong,.pcp-stock-modern-grid em{display:block}.pcp-stock-modern-grid strong{font-size:25px;margin-top:16px}.pcp-stock-modern-grid small,.pcp-stock-modern-grid em{color:#637985;font-size:11px}.pcp-stock-modern-grid em{margin-top:5px;font-style:normal}.pcp-capacity-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.pcp-capacity-grid article{padding:15px}.pcp-capacity-grid span,.pcp-capacity-grid strong,.pcp-capacity-grid small{display:block}.pcp-capacity-grid span{color:#2D8DB8;font-weight:900;font-size:11px}.pcp-capacity-grid strong{font-size:19px;margin:6px 0}.pcp-capacity-grid small{color:#637985}.pcp-planning-strip{display:flex;align-items:center;gap:12px;flex-wrap:wrap;background:#123B50;color:#fff;border-radius:10px;padding:12px 14px}.pcp-planning-strip>div{margin-right:auto}.pcp-planning-strip strong,.pcp-planning-strip span{display:block}.pcp-planning-strip span{color:#C9E2E8;font-size:11px;margin-top:2px}.pcp-timeline-wrap{display:grid;gap:12px}.pcp-timeline-scroll{overflow-x:auto}.pcp-timeline-grid{display:grid;min-width:1560px}.pcp-timeline-corner{background:#123B50;color:#fff;padding:12px;font-size:10px;font-weight:900}.pcp-day-head{background:#EAF7FA;border-left:1px solid #D2E2E8;padding:9px 7px;text-align:center}.pcp-day-head strong,.pcp-day-head small{display:block}.pcp-day-head strong{text-transform:uppercase;font-size:11px}.pcp-day-head small{color:#607985;font-size:9px}.pcp-machine-label{grid-column:1;background:#F7FBFC;border-top:1px solid #D7E3EA;padding:11px;min-height:90px;box-sizing:border-box}.pcp-machine-label strong,.pcp-machine-label span,.pcp-machine-label small{display:block}.pcp-machine-label strong{font-size:15px}.pcp-machine-label span{font-weight:800;font-size:11px}.pcp-machine-label small{color:#607985;font-size:10px}.pcp-machine-track{position:relative;min-height:105px;border-top:1px solid #D7E3EA;background:#fff;overflow:hidden}.pcp-day-lines{position:absolute;inset:0;display:grid;grid-template-columns:repeat(14,1fr);pointer-events:none}.pcp-day-lines i{border-left:1px dashed #D7E3EA}.pcp-gantt-bar{position:absolute;min-width:90px;height:29px;border:1px solid #2D8DB8;border-radius:6px;background:#D9F1F7;color:#123B50;padding:4px 7px;overflow:hidden;cursor:pointer}.pcp-gantt-bar strong,.pcp-gantt-bar span,.pcp-gantt-bar small{display:block;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.pcp-gantt-bar strong{font-size:10px}.pcp-gantt-bar span{font-size:9px;font-weight:800}.pcp-gantt-bar small{font-size:8px;color:#48616C}.pcp-info{display:flex;gap:10px;align-items:flex-start;background:#FFF8E7;border:1px solid #E9D28D;color:#5D4B1C;border-radius:10px;padding:12px;margin-top:12px}.pcp-modal-backdrop{position:fixed;inset:0;background:rgba(8,25,34,.58);z-index:1000;display:flex;align-items:center;justify-content:center;padding:20px}.pcp-modal{width:min(900px,100%);max-height:90vh;overflow:auto;background:#fff;border-radius:14px;box-shadow:0 30px 80px rgba(0,0,0,.3);padding:20px}.pcp-modal-head{display:flex;align-items:center;justify-content:space-between;gap:14px;margin-bottom:16px}.pcp-modal-head h2{margin:0;font-size:22px}.pcp-modal-close{border:0;background:#F1F5F7;border-radius:8px;width:38px;height:38px;cursor:pointer}.pcp-help-modal{display:grid;gap:7px}.pcp-help-modal button{display:grid;grid-template-columns:28px 1fr 20px;gap:10px;align-items:start;text-align:left;border:1px solid #D7E3EA;background:#F8FBFC;border-radius:9px;padding:11px;cursor:pointer}.pcp-help-modal button strong{font-size:14px}.pcp-help-modal button p{margin:3px 0;color:#637985;line-height:1.4}.pcp-help-modal button small{color:#607985}@media(max-width:1100px){.pcp-sidebar{width:215px;min-width:215px}.pcp-dashboard-grid{grid-template-columns:1fr 1fr}.pcp-dashboard-grid .pcp-dash-card:last-child{grid-column:1/-1}.pcp-production-modern{grid-template-columns:1fr}.pcp-capacity-grid{grid-template-columns:repeat(2,minmax(0,1fr))}}@media(max-width:800px){.pcp-industrial-shell{display:block}.pcp-sidebar{position:relative;width:100%;min-width:0;height:auto;max-height:330px}.pcp-side-spacer{display:none}.pcp-topbar{height:auto;min-height:58px;align-items:flex-start;flex-direction:column;padding:10px 14px}.pcp-top-actions{width:100%;overflow:auto}.pcp-page-head,.pcp-content{padding-left:14px;padding-right:14px}.quality-kpis,.pcp-dashboard-grid,.pcp-dashboard-tables,.pcp-flow,.pcp-capacity-grid{grid-template-columns:1fr}.pcp-form-grid-modern,.pcp-modal-form{grid-template-columns:1fr}.pcp-content{padding-bottom:20px}}
`}</style>
   </div>
  </section>
 </main>
}


type ProgramView=Program & {op?:OP;product?:Product}
function ProgramacaoTimeline({programs,machines,molds,employees,onProgram}:{programs:ProgramView[];machines:Machine[];molds:Molde[];employees:Employee[];onProgram:(opId?:string)=>void}){
 const active=programs.filter(p=>p.status?.toLowerCase()!=='cancelada'&&p.inicio_planejado&&p.fim_planejado)
 const base=active.length?new Date(Math.min(...active.map(p=>new Date(p.inicio_planejado).getTime()))):new Date()
 base.setHours(0,0,0,0)
 const horizon=14
 const days=Array.from({length:horizon},(_,i)=>{const d=new Date(base);d.setDate(base.getDate()+i);return d})
 const endH=new Date(base);endH.setDate(base.getDate()+horizon)
 const totalMs=endH.getTime()-base.getTime()
 const machineStats=machines.map(m=>{const ps=active.filter(p=>p.maquina_id===m.id);const pieces=ps.reduce((s,p)=>s+Number(p.quantidade_planejada||0),0);const moldCount=new Set(ps.map(p=>p.molde_id).filter(Boolean)).size;const hours=ps.reduce((s,p)=>s+Math.max(0,(new Date(p.fim_planejado).getTime()-new Date(p.inicio_planejado).getTime())/3600000),0);const shiftHours=ps.reduce((s,p)=>s+Math.max(1,Number(p.horas_turno||8))*Math.max(1,Number(p.turnos||1)),0)/Math.max(1,ps.length);return{m,ps,pieces,moldCount,hours,days:hours/Math.max(1,shiftHours)} })
 const fmtDay=(d:Date)=>d.toLocaleDateString('pt-BR',{weekday:'short',day:'2-digit',month:'2-digit'}).replace('.','')
 const durationDays=(p:ProgramView)=>Math.max(.01,(new Date(p.fim_planejado).getTime()-new Date(p.inicio_planejado).getTime())/86400000)
 const leftPct=(p:ProgramView)=>Math.max(0,Math.min(100,(new Date(p.inicio_planejado).getTime()-base.getTime())/totalMs*100))
 const widthPct=(p:ProgramView)=>Math.max(.8,Math.min(100-leftPct(p),(new Date(p.fim_planejado).getTime()-new Date(p.inicio_planejado).getTime())/totalMs*100))
 const moldName=(id:string|null|undefined)=>molds.find(m=>m.id===id)?.codigo||'sem molde'
 const empName=(id:string|null|undefined)=>employees.find(e=>e.id===id)?.nome||'—'
 return <section className="pcp-timeline-wrap">
  <div className="pcp-section-toolbar"><div><strong>Programação visual da fábrica</strong><small>Os dias ficam no topo e cada barra mostra o que está programado em cada máquina.</small></div><button className="primary-v2" onClick={()=>onProgram()}><InlineIcon name="Plus" size={17}/> Programar OP</button></div>
  <div className="pcp-planning-strip"><div><strong>O PCP calcula carga, não só datas.</strong><span>Quantidade • ciclo • cavidades • setup • turnos • eficiência • molde • operadores.</span></div><b>{active.reduce((s,p)=>s+Number(p.quantidade_planejada||0),0).toLocaleString('pt-BR')} peças programadas</b><b>{new Set(active.map(p=>p.molde_id).filter(Boolean)).size} moldes</b><b>{active.length} blocos</b></div>
  <div className="pcp-timeline-card"><div className="pcp-timeline-scroll"><div className="pcp-timeline-grid" style={{gridTemplateColumns:`190px repeat(${horizon},minmax(92px,1fr))`}}>
   <div className="pcp-timeline-corner">MÁQUINA</div>{days.map(d=><div className="pcp-day-head" key={d.toISOString()}><strong>{fmtDay(d)}</strong><small>{d.toLocaleDateString('pt-BR',{year:'numeric'})}</small></div>)}
   {machines.map(m=>{const stat=machineStats.find(x=>x.m.id===m.id)!;return <div className="pcp-machine-row" key={m.id}><div className="pcp-machine-label"><strong>{m.codigo}</strong><span>{m.nome}</span><small>{stat.ps.length} blocos • {stat.pieces.toLocaleString('pt-BR')} peças • {stat.moldCount} moldes • {stat.hours.toFixed(1)} h</small></div><div className="pcp-machine-track" style={{gridColumn:`2 / span ${horizon}`}}><div className="pcp-day-lines">{days.map(d=><i key={d.toISOString()}/>)}</div>{stat.ps.map((p:ProgramView,i:number)=><button key={p.id} className="pcp-gantt-bar" style={{left:`${leftPct(p)}%`,width:`${widthPct(p)}%`,top:`${8+(i%3)*34}px`}} onClick={()=>onProgram(p.ordem_producao_id||undefined)} title={`${p.op?.numero_op||'OP'} • ${p.product?.codigo||'produto'} • ${moldName(p.molde_id)} • ${durationDays(p).toFixed(1)} dias • ${Number(p.quantidade_planejada||0).toLocaleString('pt-BR')} peças`}><strong>{p.op?.numero_op||'OP'}</strong><span>{p.product?.codigo||'produto'} • {moldName(p.molde_id)}</span><small>{durationDays(p).toFixed(1)} d • {Number(p.quantidade_planejada||0).toLocaleString('pt-BR')} pç • {empName(p.operador_frente_id)} / {empName(p.operador_atras_id)}</small></button>)}</div></div>})}
  </div></div></div>
  <div className="pcp-capacity-summary"><div><strong>Resumo por máquina</strong><span>Mostra quantos dias a prensa fica ocupada, quantos moldes passam por ela e quantas peças estão programadas.</span></div>{machineStats.map(s=><article key={s.m.id}><strong>{s.m.codigo}</strong><b>{s.days.toFixed(1)} dias</b><span>{s.hours.toFixed(1)} h ocupadas</span><span>{s.moldCount} moldes • {s.pieces.toLocaleString('pt-BR')} peças</span></article>)}</div>
  <Table title="Detalhamento das programações" cols={['OP','Máquina','Molde','Operadores frente / atrás','Início','Fim','Dias','Peças','Ciclo','Cav.','Setup','Status']} rows={active.map((p:ProgramView)=>[p.op?.numero_op||'—',machines.find(m=>m.id===p.maquina_id)?.codigo||'—',moldName(p.molde_id),empName(p.operador_frente_id)+' / '+empName(p.operador_atras_id),new Date(p.inicio_planejado).toLocaleString('pt-BR'),new Date(p.fim_planejado).toLocaleString('pt-BR'),durationDays(p).toFixed(1),Number(p.quantidade_planejada||0),Number(p.ciclo_seg||0),Number(p.cavidades_ativas||1),Number(p.setup_min||0),p.status])} search="" setSearch={()=>{}}/>
 </section>
}

function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:ReactNode}){
 return <div className="pcp-modal-backdrop" role="dialog" aria-modal="true"><div className="pcp-modal"><div className="pcp-modal-head"><h2>{title}</h2><button className="pcp-modal-close" onClick={onClose} aria-label="Fechar"><InlineIcon name="X" size={20}/></button></div>{children}</div></div>
}

function NovaOPScreen({products,clients,machines,fichas,fitems,fichaOps,onCancel,onSave,busy,form,setForm}:{products:Product[];clients:Client[];machines:Machine[];fichas:Ficha[];fitems:FItem[];fichaOps:FichaOp[];onCancel:()=>void;onSave:(e:FormEvent)=>void;busy:boolean;form:{produto_id:string;cliente_id:string;quantidade:string;data_emissao:string;data_prevista:string;status:string;prioridade:string;observacoes:string};setForm:React.Dispatch<React.SetStateAction<{produto_id:string;cliente_id:string;quantidade:string;data_emissao:string;data_prevista:string;status:string;prioridade:string;observacoes:string}>>}){const product=products.find(p=>p.id===form.produto_id);const ficha=fichas.find(f=>f.produto_id===form.produto_id);const materials=ficha?fitems.filter(i=>i.ficha_id===ficha.id):[];const route=ficha?fichaOps.filter(o=>o.ficha_id===ficha.id).sort((a,b)=>a.sequencia-b.sequencia):[];const qty=Number(form.quantidade||0);return <form className="pcp-newop" onSubmit={onSave}><article className="pcp-newop-card"><header><b>1</b><strong>DADOS BÁSICOS DA ORDEM</strong></header><div className="pcp-newop-grid"><label>Nº da OP<input value="Será gerado automaticamente" disabled/></label><label>Data de Emissão<input type="date" value={form.data_emissao} onChange={e=>setForm(v=>({...v,data_emissao:e.target.value}))}/></label><label>Status<select value={form.status} onChange={e=>setForm(v=>({...v,status:e.target.value}))}><option value="pendente">Em Rascunho</option><option value="Planejada">Planejada</option><option value="Aguardando PCP">Aguardando PCP</option></select></label><label>Produto<select required value={form.produto_id} onChange={e=>setForm(v=>({...v,produto_id:e.target.value}))}><option value="">Selecione o produto</option>{products.map(p=><option key={p.id} value={p.id}>{p.codigo} — {p.nome}</option>)}</select></label><label>Cliente<select value={form.cliente_id} onChange={e=>setForm(v=>({...v,cliente_id:e.target.value}))}><option value="">Sem cliente vinculado</option>{clients.map(c=><option key={c.id} value={c.id}>{c.codigo?c.codigo+' — ':''}{c.nome}</option>)}</select></label><label>Prioridade<select value={form.prioridade} onChange={e=>setForm(v=>({...v,prioridade:e.target.value}))}><option value="ALTA">Alta</option><option value="REGULAR">Regular</option><option value="BAIXA">Baixa</option></select></label><label>Quantidade<input required type="number" min="0.0001" step="any" value={form.quantidade} onChange={e=>setForm(v=>({...v,quantidade:e.target.value}))}/></label><label>Data prevista<input type="date" value={form.data_prevista} onChange={e=>setForm(v=>({...v,data_prevista:e.target.value}))}/></label><label className="wide">Observações<textarea rows={2} value={form.observacoes} onChange={e=>setForm(v=>({...v,observacoes:e.target.value}))}/></label></div></article><article className="pcp-newop-card"><header><b>2</b><strong>MATÉRIAS-PRIMAS NECESSÁRIAS — EXPLOSÃO DE MATERIAIS / BOM</strong></header><div className="pcp-newop-help">{product?<>Produto selecionado: <strong>{product.codigo} — {product.nome}</strong> • Ficha técnica {ficha?'v'+ficha.versao:'não cadastrada'}. Quantidade da OP: <strong>{qty.toLocaleString('pt-BR')}</strong>.</>:<>Selecione o produto para calcular a necessidade pela ficha técnica ativa.</>}</div><table className="pcp-newop-table"><thead><tr><th>Item</th><th>Qtd. necessária</th><th>Estoque disponível</th><th>Situação</th></tr></thead><tbody>{materials.map(i=>{const p=products.find(x=>x.id===i.componente_id);const need=Number(i.quantidade)*qty*(1+Number(i.perda_percentual||0)/100);const stock=Number(p?.estoque_atual||0);return <tr key={i.id}><td>{p?.codigo||'—'} — {p?.nome||'Componente não localizado'}</td><td>{need.toLocaleString('pt-BR')} {i.unidade_medida||'UN'}</td><td>{stock.toLocaleString('pt-BR')} {i.unidade_medida||'UN'}</td><td className={stock>=need?'pcp-newop-ok':'pcp-newop-warn'}>{stock>=need?'OK':'FALTA'}</td></tr>})}{!materials.length&&<tr><td colSpan={4}>Nenhuma estrutura BOM real cadastrada para o produto selecionado.</td></tr>}</tbody></table></article><article className="pcp-newop-card"><header><b>3</b><strong>ROTEIRO DE PRODUÇÃO — FLUXO DE MÁQUINAS</strong></header><div className="pcp-newop-route">{route.map(o=>{const m=machines.find(x=>x.id===o.maquina_id);return <div className="pcp-newop-route-row" key={o.id}><b>Seq {o.sequencia}</b><strong>{o.operacao}</strong><span>Setup: {Number(o.setup_min||0).toLocaleString('pt-BR')} min • Ciclo: {Number(o.ciclo_seg||0).toLocaleString('pt-BR')} s</span><span>C.T.: {m?.codigo||'Não definido'}</span></div>})}{!route.length&&<div className="pcp-newop-help">Nenhum roteiro real cadastrado na ficha técnica deste produto.</div>}</div></article><article className="pcp-newop-card"><header><b>4</b><strong>DESTINO DA INFORMAÇÃO</strong></header><div className="pcp-newop-actions"><button type="button" onClick={onCancel}>✕ Cancelar</button><button className="primary" disabled={busy} type="submit">💾 {busy?'SALVANDO…':'SALVAR E GERAR OP'}</button></div></article></form>}\nfunction Table({title,cols,rows,search,setSearch}:{title:string;cols:string[];rows:(string|number)[][];search:string;setSearch:(v:string)=>void}){
 return <section className="crud-list"><div className="crud-list-head"><div><strong>{title}</strong><small>{rows.length} registros</small></div><div className="module-search"><InlineIcon name="Search" size={16}/><input placeholder="Pesquisar..." value={search} onChange={e=>setSearch(e.target.value)}/></div></div><div className="crud-table-wrap"><table><thead><tr>{cols.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{rows.map((r,i)=><tr key={i}>{r.map((v,j)=><td key={j}>{String(v??'—')}</td>)}</tr>)}{!rows.length&&<tr><td colSpan={cols.length} className="crud-empty">Nenhum registro encontrado.</td></tr>}</tbody></table></div></section>
}
