/* REVISÃO DE ENGENHARIA | Projeto: SGQ ERP Industrial | Pasta: src/ | Arquivo: IndustrialLoginDirect.tsx | Data: 2026-09-17 | Correções: login curto -> domínio corporativo; provisionamento server-side de contas oficiais via Edge Function; sessão Auth; validação ERP; Launchpad após sessão | Segurança: senha permanece somente no formulário e é enviada por HTTPS à Edge Function; service_role permanece no servidor; nenhum INSERT em auth.users pelo frontend; RLS não é desativado | Testes/Homologação: revisão estática; E2E pendente de execução em navegador | Status: REVISADO — NÃO HOMOLOGADO */

import { FormEvent, useEffect, useState } from 'react'
import { ArrowRight, Building2, Eye, EyeOff, KeyRound, LogIn, ShieldCheck, UserPlus } from 'lucide-react'
import { supabase, supabaseConfigurado } from './lib/supabaseClient'
import './styles/industrial-login.css'

type Props = { returnTo?: string; masterMode?: boolean }
function safeReturnTo(value: string | null | undefined) { if (!value || !value.startsWith('/') || value.startsWith('//') || value.startsWith('/login')) return '/erp-industrial'; return value }
function normalizeIndustrialLogin(value: string) { const normalized=value.trim().toLowerCase(); return normalized.includes('@') ? normalized : `${normalized}@transforbater.com.br` }
async function validateIndustrialSession(userId:string){
 const {data:profile,error:profileError}=await supabase.from('erp_usuarios').select('id,auth_user_id,empresa_id,nivel_admin,ativo,is_master,role,deleted_at,setor_id').eq('auth_user_id',userId).eq('ativo',true).is('deleted_at',null).maybeSingle();
 if(profileError)throw profileError;if(!profile)throw new Error('Usuário autenticado, mas sem perfil ERP ativo.');if(profile.auth_user_id!==userId)throw new Error('O vínculo entre Supabase Auth e o perfil ERP é inválido.');if(!profile.empresa_id)throw new Error('Usuário autenticado sem empresa industrial vinculada.');
 const {data:empresa,error:empresaError}=await supabase.from('erp_empresas').select('id,razao_social,nome_fantasia,ativo').eq('id',profile.empresa_id).eq('ativo',true).maybeSingle();if(empresaError)throw empresaError;if(!empresa)throw new Error('A empresa vinculada ao usuário está inexistente ou inativa.');return {profile,empresa}
}
async function executarBootstrapOficial(email:string,password:string){
 const {data,error}=await supabase.functions.invoke('erp-login',{body:{action:'bootstrap_official',email,password}})
 if(error)throw error
 if(!data?.ok)throw new Error(String(data?.error??'Não foi possível provisionar o acesso oficial.'))
 return data
}
export default function IndustrialLoginDirect({returnTo,masterMode=false}:Props){
 const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[showPassword,setShowPassword]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[notice,setNotice]=useState(''),[checking,setChecking]=useState(true)
 useEffect(()=>{let alive=true;async function bootstrap(){if(!supabaseConfigurado){if(alive)setChecking(false);return}try{const{data,error:sessionError}=await supabase.auth.getSession();if(sessionError)throw sessionError;const user=data.session?.user;if(!user){if(alive)setChecking(false);return}await validateIndustrialSession(user.id);if(alive)window.location.replace(safeReturnTo(returnTo))}catch(err){console.error('[ERP login bootstrap]',err);await supabase.auth.signOut().catch(()=>undefined);if(alive){setError('A sessão anterior não possui acesso válido ao ERP Industrial. Entre novamente com uma conta Industrial autorizada.');setChecking(false)}}}void bootstrap();return()=>{alive=false}},[returnTo])
 async function submit(event:FormEvent){event.preventDefault();setError('');setNotice('');if(!supabaseConfigurado){setError('O ambiente do ERP não está configurado. Verifique VITE_SUPABASE_URL e VITE_SUPABASE_PUBLISHABLE_KEY na Vercel e faça um novo deploy.');return}if(!email.trim()||!password){setError('Informe seu e-mail corporativo e sua senha.');return}const emailTratado=normalizeIndustrialLogin(email);setEmail(emailTratado);setBusy(true);try{
   let{data,error:authError}=await supabase.auth.signInWithPassword({email:emailTratado,password})
   if(authError&&/invalid login credentials/i.test(authError.message)){
     try{await executarBootstrapOficial(emailTratado,password);({data,error:authError}=await supabase.auth.signInWithPassword({email:emailTratado,password}))}catch(bootstrapError){console.warn('[ERP official bootstrap]',bootstrapError)}
   }
   if(authError)throw authError;if(!data.session?.user)throw new Error('O Supabase não retornou uma sessão válida.');const{data:verifiedUser,error:userError}=await supabase.auth.getUser();if(userError||!verifiedUser.user)throw userError??new Error('O usuário autenticado não pôde ser validado.');const{profile,empresa}=await validateIndustrialSession(verifiedUser.user.id);const role=String(profile.role??'').trim().toUpperCase(),level=Number(profile.nivel_admin??0),master=Boolean(profile.is_master)||level>=9||['MASTER','MASTER_ADMIN','SUPER_ADMIN'].includes(role);if(masterMode&&!master)throw new Error('Este acesso não possui perfil MASTER autorizado.');setNotice(`Acesso validado para ${empresa.nome_fantasia||empresa.razao_social||'PLASTIBOR'}. ${master?'Perfil MASTER.':'Perfil autorizado.'} Abrindo o ERP…`);const{data:finalSession,error:finalSessionError}=await supabase.auth.getSession();if(finalSessionError||finalSession.session?.user.id!==verifiedUser.user.id)throw finalSessionError??new Error('A sessão não ficou disponível após a validação.');window.location.replace(safeReturnTo(returnTo))
 }catch(err){console.error('[ERP login]',err);await supabase.auth.signOut().catch(()=>undefined);const message=err instanceof Error?err.message:'Não foi possível entrar no ERP.';const friendly=/invalid login credentials/i.test(message)?'E-mail ou senha incorretos.':/email not confirmed/i.test(message)?'O e-mail ainda não foi confirmado no Supabase.':message;setError(friendly)}finally{setBusy(false)}}
 async function recover(){setError('');setNotice('');if(!supabaseConfigurado){setError('O ambiente Supabase não está configurado na Vercel.');return}if(!email.trim()){setError('Informe seu e-mail corporativo para receber o link de recuperação.');return}setBusy(true);try{const{error:recoveryError}=await supabase.auth.resetPasswordForEmail(normalizeIndustrialLogin(email),{redirectTo:`${window.location.origin}/login?reset=1`});if(recoveryError)throw recoveryError;setNotice('Se o e-mail estiver cadastrado, as instruções de recuperação serão enviadas.')}catch(err){setError(err instanceof Error?err.message:'Não foi possível solicitar a recuperação.')}finally{setBusy(false)}}
 if(checking)return <main className="auth-screen"><section className="auth-panel"><div className="auth-panel-inner"><div className="auth-heading"><span className="auth-overline">{masterMode?'SGQ ERP INDUSTRIAL • LOGIN MASTER':'PLASTIBOR • ACESSO SEGURO'}</span></div><h1>Preparando seu acesso</h1><p className="auth-description">Validando a sessão do SGQ ERP Industrial…</p><div className="auth-security-note"><ShieldCheck size={16}/><span>Sessão segura e persistente.</span></div></div></section></main>
 return <main className="auth-screen"><section className="auth-visual" aria-label="SGQ ERP Industrial"><img src="/images/sgq/sgq-erp-login.png" alt="Ambiente industrial do SGQ ERP"/><div className="auth-visual-shade"/><div className="auth-visual-grid" aria-hidden="true"/><div className="auth-visual-content"><a href="/" className="auth-visual-logo"><img src="/logo-industrial.svg" alt="SGQ ERP"/></a><div className="auth-visual-message"><span>PLASTIBOR • SGQ ERP INDUSTRIAL</span><h2>Uma fábrica inteira.<br/><em>Um único controle.</em></h2><p>PCP, produção, qualidade, estoque, manutenção, financeiro e fiscal trabalhando sobre os mesmos dados.</p><div className="auth-trust"><b><ShieldCheck size={14}/> Sessão segura</b><b><Building2 size={14}/> Multiempresa</b></div></div><small>FernandoSch_System</small></div></section><section className="auth-panel"><div className="auth-panel-inner"><div className="auth-mobile-brand"><a href="/"><img src="/logo-industrial.svg" alt="SGQ ERP"/></a></div><div className="auth-heading"><span className="auth-overline">PLASTIBOR • ACESSO SEGURO</span><span className="auth-status"><i/> Ambiente protegido</span></div><h1>{masterMode?'Acesso do Proprietário Master':'Entrar no SGQ ERP'}</h1><p className="auth-description">{masterMode?'Use as credenciais do proprietário Master. O perfil MASTER é validado antes da abertura do painel.':'Use as credenciais cadastradas. A empresa, o perfil e o status são verificados antes da entrada.'}</p><form className="auth-form" onSubmit={submit}><label htmlFor="erp-direct-email">E-mail corporativo</label><div className="auth-input-wrap"><input id="erp-direct-email" type="text" value={email} onChange={e=>setEmail(e.target.value)} placeholder="seu@email.com ou usuário curto" autoComplete="username" autoFocus required/></div><div className="auth-label-row"><label htmlFor="erp-direct-password">Senha</label><button type="button" className="auth-text-button" onClick={recover} disabled={busy}>Esqueci minha senha</button></div><div className="auth-input-wrap"><input id="erp-direct-password" type={showPassword?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Digite sua senha" autoComplete="current-password" required/><button type="button" className="auth-password-toggle" aria-label={showPassword?'Ocultar senha':'Mostrar senha'} onClick={()=>setShowPassword(v=>!v)}>{showPassword?<EyeOff size={18}/>:<Eye size={18}/>}</button><KeyRound className="auth-key-icon" size={16} aria-hidden="true"/></div>{error&&<div className="auth-message auth-error" role="alert">{error}</div>}{notice&&<div className="auth-message auth-notice" role="status">{notice}</div>}<button className="auth-submit" type="submit" disabled={busy}>{busy?<><span className="auth-spinner"/>Entrando…</>:<>Entrar no sistema <LogIn size={18}/></>}</button><div className="auth-divider"><span>ou</span></div><a className="auth-register" href="/cadastro-empresa"><UserPlus size={18}/> Criar uma nova empresa</a><a className="auth-trial" href="/cadastro-empresa">Começar teste grátis de 15 dias <ArrowRight size={16}/></a></form><div className="auth-security-note"><ShieldCheck size={16}/><span>Autenticação feita pelo Supabase Auth. Nenhuma senha é armazenada no navegador.</span></div><div className="auth-footer"><a href="/">Voltar para o site</a><span>•</span><a href="/contato">Fale conosco</a></div></div></section></main>
}
